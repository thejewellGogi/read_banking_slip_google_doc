<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ✅ สำหรับ Android 14/15+ : ใช้กับกรณีต้องเข้าถึงรูปที่ผู้ใช้เลือกไว้ถาวร -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- ✅ สำหรับ Android 13+ (API 33–34–35) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- ✅ เผื่ออุปกรณ์ Android < 13 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- ✅ ถ่ายภาพด้วยกล้องผ่าน image_picker (ถ้าใช้ถ่ายรูป) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- (ไม่บังคับ) บอกว่ากล้องเป็น optional -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:label="read_banking_slip"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Flutter plugin registry -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- คงไว้ตามของเดิม -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
