class ExpenseModel {
  final DateTime dateTime;
  final String title;
  final double amount;

  const ExpenseModel({
    required this.dateTime,
    required this.title,
    required this.amount,
  });
}
